## ADDED Requirements

### Requirement: Lunch Configuration
The system SHALL allow users to configure global settings for Lunch events.

#### Scenario: Configuring Lunch
- **WHEN** viewing the Event Types Settings (or dedicated Lunch Settings)
- **THEN** user can select a default Target Calendar for lunch events
- **AND** user can enter a default Invitee Email
